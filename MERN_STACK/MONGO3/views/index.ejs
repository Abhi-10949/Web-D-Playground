<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini WhatsApp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h2>All chats</h2>
    <hr>
    <form method="get" action="/chats/new">
        <button>New Chat</button>
    </form>
    <br>
    <% for(chat of chats){ %>
    <div class="chat">
        <p>From: <i><%= chat.from %></i></p>
        <div class="msg">
            <p><%= chat.msg %></p>
            <form method="get" action="/chats/<%= chat._id %>/edit">
                <button>edit</button>
            </form>
        </div>
        <p>Recived By: <i><%= chat.to %></i></p>
        <hr>
        <p>Time: <%= chat.created_at.toString().split(" ")[4] %></p> <!-- toLocaleTimeString() -->
        <p>Date: <%= chat.created_at.toLocaleDateString() %></p>
    </div>
    <br><br>
    <% } %>
</body>
</html>